# URL of the FastAPI backend — required in production
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: bearer token matching API_SECRET_KEY on the backend.
# NOTE: this value is bundled into client JS and visible in the browser.
# Suitable only for personal/private deployments where obscurity is enough.
NEXT_PUBLIC_API_KEY=
